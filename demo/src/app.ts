// A tiny fake app so the healthy doctors have something to find.
export function greet(name: string) {
  // TODO: personalize the greeting once profiles land
  console.log("hello " + name);
  return "hello " + name;
}
