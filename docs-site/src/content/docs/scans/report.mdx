---
title: The report
description: Anatomy of a scan — findings, blind spots, score, and what a broken run looks like.
audience: human
sidebar:
  order: 3
---

A run ends with a report. It is built to be read once, at a glance, and
to never overstate what it knows.

## Findings

Every finding carries:

- **The claim** — the observable condition the check states, e.g.
  "Fetch call does not provide an AbortSignal."
- **The check** — `doctor/check-id`, the rule that fired.
- **The location** — `file:line`, and the count when the same shape
  appears more than once.
- **Severity** — `error`, `warning`, or `info`.

Checks also explain themselves: `impact` (what goes wrong), `why` (the
mechanism), and `fix` (what to do). These fields ride along in the
[JSON output](/agents/json-reference) so your agent reads the same
explanations you do.

## Blind spots

Every doctor declares what it cannot see, and the report prints those
blind spots next to the findings. A doctor that cannot tell whether a
variable supplies an AbortSignal says exactly that. Findings arrive
pre-annotated with their limits — you never have to guess how a sausage
was made.

## The score

A 0–100 number at the end of a run. Severity is weighted across findings
and files, with a grade:

| Range | Grade |
| ----- | ----- |
| 90–100 | Excellent |
| 75–89 | Good |
| 50–74 | Fair |
| 25–49 | Poor |
| 0–24 | Critical |

Two honest exceptions:

- An **empty scan** (zero files checked) never scores a perfect 100. A
  vacuous pass is a warning, not a win.
- A **partial scan** (a crashed or broken doctor) withholds the grade.
  Incomplete results never get to look Excellent.

## Broken doctors are infrastructure failures

A doctor that fails to load or crashes mid-scan is reported as broken,
fails the run outright, and the score renders as null. Broken tooling
never reads as a clean codebase.
